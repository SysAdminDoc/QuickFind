# QuickFind runtime dependencies pinned to the tested Windows build matrix.
PyQt6==6.11.0
PyQt6-Qt6==6.11.1
PyQt6-sip==13.11.1
pdfplumber==0.11.10
# Pinned explicitly (transitive via pdfplumber) to stay at/above the fix for
# CVE-2025-64512 (arbitrary code execution from a crafted PDF CMap).
pdfminer.six==20251230
py7zr==1.1.3
pywin32==312
python-docx==1.2.0
python-pptx==1.0.2
watchdog==6.0.0

# Local build/test tooling.
pyinstaller==6.21.0
pytest==9.0.3
